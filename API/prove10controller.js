function db_display(req, res) {
  // var user = req.query.user//use this later to get which user maybe?
  
  res.render('./pages/prove10result')//, {weight: weight, type: type, cost: cost})
}

module.exports = db_display